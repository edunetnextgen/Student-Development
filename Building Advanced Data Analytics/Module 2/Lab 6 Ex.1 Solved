You have the temperature data for a week. Create a line plot to visualize the temperature trend over the week. Customize the plot with different colors, markers, and line styles.
-------------------------------------

import matplotlib.pyplot as plt

# Days of the week
days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']

# Temperature data for the week
temperatures = [25, 27, 24, 22, 23, 26, 28]

# Create a line plot with customized style
plt.plot(days, temperatures, color='blue', marker='o', linestyle='--', linewidth=2, markersize=8, label='Temperature')

# Add labels and title
plt.xlabel('Days')
plt.ylabel('Temperature (°C)')
plt.title('Temperature Trend over the Week')
plt.legend()

# Customize grid and tick marks
plt.grid(True, linestyle='--', alpha=0.7)
plt.xticks(rotation=45)

# Show the plot
plt.show()
